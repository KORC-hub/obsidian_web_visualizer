# Configuracion

## Mis comandos de configuracion Inicial
### Copiar rapido 

```bash
git config --global user.name "Kevin Rogers C." && git config --global user.email "72825328+KORC-hub@users.noreply.github.com" && git config --global init.defaultBranch main && git config --global core.editor "code --wait" && git config --global color.ui auto && git config --global push.default current && git config --global alias.lg "log --oneline --graph --all --decorate"
```

### Cada Comando

```bash
git config --global user.name "Kevin Rogers C."
```

```bash
git config --global user.email "72825328+KORC-hub@users.noreply.github.com"
```

```bash
git config --global init.defaultBranch main
```

```bash
git config --global core.editor "code --wait"
```

```bash
git config --global color.ui auto
```

```bash
git config --global push.default current
```

```bash
git config --global alias.lg "log --oneline --graph --all --decorate"
```

## Otros comandos de configuracion

```bash
git config --global pull.rebase true
```