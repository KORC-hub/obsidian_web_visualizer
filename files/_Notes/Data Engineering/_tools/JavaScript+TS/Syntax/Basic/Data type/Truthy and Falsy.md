Tag: #data-type #booleans

En JavaScript, un valor debe ser falso o verdadero, la siguiente lista contiene todos los posibles valores falsos en JavaScript, todos los demás se considera verdadero.

```js
const falsy = [false, 0, -0, "", null, undefined, NaN, 0n, document.all]
```